name: as-video-generator

services:
  - name: telegram-bot
    type: worker
    runtime: nodejs
    build:
      command: npm install
    run:
      command: npm start
    env:
      - key: BOT_TOKEN
        value: YOUR_BOT_TOKEN
